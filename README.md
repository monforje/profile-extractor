# Profile Extractor Bot

## 📋 Содержание

1. [Что это такое](#что-это-такое)
2. [Основные возможности](#основные-возможности)
3. [Для чего используется](#для-чего-используется)
4. [Принцип работы](#принцип-работы)
5. [Технические возможности](#технические-возможности)
6. [Примеры использования](#примеры-использования)
7. [Установка и настройка](#установка-и-настройка)
8. [Использование](#использование)
9. [Конфигурация](#конфигурация)
10. [Ограничения](#ограничения)
11. [FAQ](#faq)

---

## Что это такое

**Profile Extractor Bot** — это интеллектуальная система, которая автоматически извлекает структурированную информацию из неформального текстового описания человека и создает детальный профиль в формате JSON.

### Ключевая особенность
Система использует **динамическую схему данных** через YAML-файл, что означает:
- ✅ Вы можете изменить структуру профиля простым редактированием одного файла
- ✅ Добавление новых полей не требует изменения кода
- ✅ Система автоматически адаптируется к любой схеме данных

---

## Основные возможности

### 🤖 Интеллектуальное извлечение данных
- Анализирует неструктурированный текст любой длины
- Распознает различные способы описания одной и той же информации
- Понимает контекст и подтекст сообщений

### 🔧 Динамическая схема
- Настройка через простой YAML файл
- Поддержка сложных типов данных (массивы, объекты, вложенные поля)
- Возможность создания собственных онтологий

### 🎯 Двухэтапная обработка
- **Этап 1**: Извлечение и структурирование данных
- **Этап 2**: Валидация, проверка на дубли и исправление ошибок

### 📊 Поддержка сложных структур
- Вложенные объекты через точечную нотацию (`location.city`)
- Массивы объектов (`skills`, `education`)
- Автоматическое создание тегов для дополнительной информации

---

## Для чего используется

### 👥 HR и рекрутинг
- **Обработка резюме**: Автоматическое создание структурированных профилей из текстовых резюме
- **Анализ кандидатов**: Извлечение ключевой информации для базы данных
- **Стандартизация**: Приведение всех профилей к единому формату

### 🏢 Корпоративные системы
- **Внутренние профили сотрудников**: Создание карточек персонала
- **Система компетенций**: Каталогизация навыков и опыта
- **Планирование карьеры**: Анализ целей и предпочтений сотрудников

### 💼 Консалтинг и исследования
- **Анализ экспертов**: Создание баз экспертных профилей
- **Исследование рынка труда**: Структурирование данных о специалистах
- **Профессиональные сообщества**: Каталогизация участников

### 🎓 Образование
- **Профили студентов**: Структурирование академических данных
- **Портфолио**: Создание цифровых профилей достижений
- **Карьерное консультирование**: Анализ компетенций и интересов

### 🌐 Социальные платформы
- **Профили пользователей**: Автоматическое заполнение анкет
- **Рекомендательные системы**: Структурированные данные для алгоритмов
- **Нетворкинг**: Создание профессиональных профилей

---

## Принцип работы

### Архитектура системы

```
Пользовательский текст
         ↓
    [YAML Parser] ← dictionary.yaml (схема данных)
         ↓
   [Prompt Generator] (создание инструкций для AI)
         ↓
    [OpenAI API] (GPT-4.1-mini через OpenRouter)
         ↓
   [Этап 1: Извлечение] (структурирование данных)
         ↓
   [Этап 2: Валидация] (проверка и очистка)
         ↓
   [JSON Validator] (финальная проверка)
         ↓
    profile.json (результат)
```

### Процесс обработки

#### 1. **Парсинг схемы**
Система читает `dictionary.yaml` и создает карту полей с их типами:
```yaml
name: string
age: int
skills: array
location.city: string  # точечная нотация
```

#### 2. **Генерация промпта**
Создается детальная инструкция для AI модели с:
- Описанием всех полей из схемы
- Примерами правильного форматирования
- Правилами валидации
- Инструкциями по созданию тегов

#### 3. **Первый запрос к AI**
```
Входящий текст: "Привет! Меня зовут Иван, мне 25 лет. Работаю программистом..."

Результат: {
  "name": "Иван",
  "age": 25,
  "role": {"current": "программист"},
  "tags": {"personality": "дружелюбный"}
}
```

#### 4. **Второй запрос к AI (валидация)**
- Проверка на дублирование информации между полями и тегами
- Валидация типов данных
- Исправление логических противоречий
- Очистка некорректных значений

#### 5. **Финальная проверка**
Программная валидация JSON структуры и соответствия схеме.

---

## Технические возможности

### 🔧 Поддерживаемые типы данных

| Тип | Описание | Пример |
|-----|----------|---------|
| `string` | Текстовые значения | `"Иван Петров"` |
| `int` | Целые числа | `25` |
| `array` | Массивы объектов | `[{"name": "Go", "level": "advanced"}]` |
| `object` | Произвольные объекты | `{"hobby": "фотография"}` |
| `bool` | Логические значения | `true` |
| `float` | Числа с плавающей точкой | `3.14` |

### 🏗️ Структурные возможности

#### Точечная нотация
```yaml
location.city: string
location.country: string
social.telegram: string
```
Создает:
```json
{
  "location": {
    "city": "Москва",
    "country": "Россия"
  },
  "social": {
    "telegram": "@username"
  }
}
```

#### Массивы объектов
```yaml
skills: array
education: array
```
Создает:
```json
{
  "skills": [
    {"name": "Python", "level": "advanced"},
    {"name": "JavaScript", "level": "intermediate"}
  ],
  "education": [
    {"university": "МГУ", "degree": "бакалавр", "year": 2020}
  ]
}
```

#### Автоматические теги
Информация, которая не поместилась в стандартные поля:
```json
{
  "tags": {
    "hobby_photography": "увлекается фотографией",
    "personality_communicative": "коммуникабельный",
    "goal_startup": "хочет создать стартап"
  }
}
```

### 🤖 AI модель

- **Модель**: GPT-4.1-mini (через OpenRouter)
- **Температура**: 0.1 (для максимальной точности)
- **Лимит токенов**: 2000 на ответ
- **Язык промптов**: Русский (легко адаптируется)

---

## Примеры использования

### Пример 1: Простой профиль

**Входной текст:**
```
Меня зовут Анна, мне 28 лет. Живу в Санкт-Петербурге. 
Работаю UX дизайнером в IT компании уже 4 года. 
Знаю Figma и Sketch на продвинутом уровне.
```

**Результат:**
```json
{
  "name": "Анна",
  "age": 28,
  "location": {
    "city": "Санкт-Петербург"
  },
  "role": {
    "current": "UX дизайнер",
    "experience_years": 4
  },
  "company": {
    "industry": "IT"
  },
  "skills": [
    {"name": "Figma", "level": "advanced"},
    {"name": "Sketch", "level": "advanced"}
  ]
}
```

### Пример 2: Развернутый профиль

**Входной текст:**
```
Привет! Я Дмитрий Козлов, fullstack разработчик, 32 года. 
Живу в Москве, но готов к переезду. Работаю в стартапе 
CTO уже 2 года, до этого 5 лет был senior developer. 
Знаю Python, JavaScript, React на экспертном уровне, 
Go изучаю. Закончил МФТИ в 2013. Есть сертификат AWS. 
Телеграм @dmitry_dev. Увлекаюсь скалолазанием и 
фотографией. Хочу в будущем создать свою компанию.
```

**Результат:**
```json
{
  "name": "Дмитрий Козлов",
  "age": 32,
  "location": {
    "city": "Москва"
  },
  "role": {
    "current": "CTO",
    "level": "fullstack разработчик",
    "experience_years": 7
  },
  "career": {
    "relocation_willingness": "готов к переезду",
    "goals": ["создать свою компанию"]
  },
  "education": [
    {"university": "МФТИ", "year": 2013}
  ],
  "certifications": [
    {"name": "AWS"}
  ],
  "skills": [
    {"name": "Python", "level": "expert"},
    {"name": "JavaScript", "level": "expert"},
    {"name": "React", "level": "expert"},
    {"name": "Go", "level": "beginner"}
  ],
  "social": {
    "telegram": "@dmitry_dev"
  },
  "sports": ["скалолазание"],
  "hobbies": ["фотография"],
  "tags": {
    "company_type": "стартап",
    "ambition": "предпринимательство"
  }
}
```

### Пример 3: Нестандартная информация

**Входной текст:**
```
Елена, 35 лет, HR-директор из Екатеринбурга. 
Работаю в крупной производственной компании. 
Психолог по образованию, MBA в области управления персоналом. 
Говорю на английском и немецком. Замужем, двое детей. 
Веду блог о корпоративной культуре. Практикую йогу.
```

**Результат:**
```json
{
  "name": "Елена",
  "age": 35,
  "location": {
    "city": "Екатеринбург"
  },
  "role": {
    "current": "HR-директор"
  },
  "company": {
    "industry": "производство",
    "size": "крупная"
  },
  "education": [
    {"degree": "психолог"},
    {"degree": "MBA", "specialization": "управление персоналом"}
  ],
  "languages": [
    {"name": "английский"},
    {"name": "немецкий"}
  ],
  "family": {
    "marital_status": "замужем",
    "children_count": 2
  },
  "creative": {
    "skills": ["ведение блога"]
  },
  "sports": ["йога"],
  "tags": {
    "blog_topic": "корпоративная культура",
    "expertise_area": "управление персоналом"
  }
}
```

---

## Установка и настройка

### Системные требования

- **Go**: версия 1.21 или выше
- **Интернет**: для доступа к OpenRouter API
- **ОС**: Windows, macOS, Linux

### Зависимости

```bash
go mod download
```

Автоматически установит:
- `github.com/joho/godotenv` - загрузка переменных окружения
- `gopkg.in/yaml.v2` - парсинг YAML файлов

### API ключ

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai)
2. Получите API ключ
3. Создайте файл `.env`:
```bash
OPENAI_API_KEY=your_openrouter_api_key_here
```

### Структура проекта

```
profile-extractor/
├── main.go                    # Основной файл
├── .env                       # API ключ
├── go.mod                     # Зависимости
├── config/
│   └── dictionary.yaml        # Схема данных
├── internal/
│   ├── schema/parser.go       # Парсер YAML
│   ├── prompts/generator.go   # Генератор промптов
│   ├── api/openai.go          # Клиент API
│   └── validator/json.go      # Валидатор
└── output/
    └── profile.json           # Результаты
```

---

## Использование

### Быстрый старт

1. **Клонируйте или создайте проект**
2. **Установите зависимости**: `go mod download`
3. **Настройте API ключ** в `.env`
4. **Запустите**: `go run main.go`

### Интеграция в код

```go
package main

import (
    "profile-extractor/internal/api"
    "profile-extractor/internal/schema"
    "profile-extractor/internal/prompts"
)

func main() {
    // Загрузка схемы
    schemaFields, _ := schema.ParseYAMLSchema(yamlContent)
    
    // Создание клиента
    client := api.NewOpenAIClient(apiKey)
    
    // Извлечение профиля
    prompt := prompts.GenerateExtractionPrompt(schemaFields, userText)
    profile, _ := client.ExtractProfile(prompt)
    
    // Результат готов к использованию
}
```

### REST API обертка

```go
// Пример простого веб-сервера
func extractHandler(w http.ResponseWriter, r *http.Request) {
    userText := r.FormValue("text")
    
    // Обработка через Profile Extractor
    profile := processProfile(userText)
    
    w.Header().Set("Content-Type", "application/json")
    json.NewEncoder(w).Encode(profile)
}
```

---

## Конфигурация

### Настройка схемы данных

Файл `config/dictionary.yaml` определяет структуру профиля:

#### Простые поля
```yaml
name: string
age: int
active: bool
```

#### Вложенные объекты
```yaml
location.city: string        # {"location": {"city": "..."}}
location.country: string     # {"location": {"country": "..."}}
social.email: string         # {"social": {"email": "..."}}
```

#### Массивы
```yaml
skills: array               # [{"name": "...", "level": "..."}]
education: array            # [{"university": "...", "year": ...}]
languages: array            # [{"name": "...", "level": "..."}]
```

#### Специальные типы
```yaml
tags: object               # Динамически генерируемые поля
```

### Настройка промптов

В файле `internal/prompts/generator.go`:

```go
// Изменение температуры для креативности
Temperature: 0.3  // больше креативности

// Изменение лимита токенов
MaxTokens: 4000   // для более длинных ответов
```

### Настройка валидации

В файле `internal/validator/json.go`:

```go
// Добавление собственных валидаторов
func validateCustomField(value interface{}) error {
    // Ваша логика валидации
}
```

---

## Ограничения

### Технические ограничения

- **Размер текста**: Рекомендуется до 2000 слов
- **Язык**: Оптимизировано для русского языка
- **API лимиты**: Зависят от тарифа OpenRouter
- **Интернет**: Требуется постоянное соединение

### Ограничения точности

- **Субъективная информация**: Может неточно интерпретировать мнения
- **Неполные данные**: Не придумывает отсутствующую информацию
- **Контекст**: Может не понять сложные контекстуальные связи
- **Даты**: Может путать относительные временные ссылки

### Ограничения схемы

- **Точечная нотация**: Поддерживается только один уровень вложенности
- **Сложные типы**: Нет поддержки union типов или условной валидации
- **Динамические поля**: Только через механизм `tags`

---

## FAQ

### Общие вопросы

**Q: Можно ли использовать другие AI модели?**
A: Да, достаточно изменить endpoint и модель в `internal/api/openai.go`. Поддерживается любая модель с совместимым API.

**Q: Поддерживается ли другие языки кроме русского?**
A: Да, достаточно изменить промпты в `internal/prompts/generator.go` на нужный язык.

**Q: Можно ли обрабатывать несколько профилей одновременно?**
A: Да, система поддерживает параллельную обработку. Используйте goroutines для батчевой обработки.

### Технические вопросы

**Q: Как добавить новый тип данных?**
A: Добавьте обработку в функцию `validateBasicType` в `internal/validator/json.go`.

**Q: Можно ли сохранять в другие форматы кроме JSON?**
A: Да, добавьте необходимые сериализаторы в main.go (XML, YAML, CSV и т.д.).

**Q: Как изменить логику валидации?**
A: Отредактируйте промпт в `GenerateValidationPrompt` или добавьте программную валидацию в validator.

### Производительность

**Q: Сколько времени занимает обработка одного профиля?**
A: Обычно 5-15 секунд в зависимости от размера текста и нагрузки на API.

**Q: Можно ли кэшировать результаты?**
A: Да, добавьте Redis или файловое кэширование по хэшу входного текста.

**Q: Как масштабировать на большие объемы?**
A: Используйте воркеры, очереди задач (Redis Queue) и балансировку нагрузки.

### Безопасность

**Q: Как обеспечить конфиденциальность данных?**
A: Данные передаются через HTTPS. Для критичных данных используйте локальные модели или приватные API.

**Q: Логируются ли запросы?**
A: Система логирует только технические данные. Настройте уровень логирования в зависимости от требований.

---

## Заключение

Profile Extractor Bot — мощная и гибкая система для автоматизации создания структурированных профилей из текстовых описаний. Система идеально подходит для HR, корпоративных платформ, исследований и любых задач, требующих стандартизации персональных данных.

**Ключевые преимущества:**
- 🚀 Быстрая настройка и интеграция
- 🔧 Полная кастомизация через YAML
- 🤖 Высокая точность благодаря AI
- 📊 Поддержка сложных структур данных
- 🔍 Автоматическая валидация и очистка

Система готова к продакшен использованию и может обрабатывать тысячи профилей с минимальными доработками.